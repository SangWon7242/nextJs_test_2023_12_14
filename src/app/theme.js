import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff8686",
      contrastText: "#fff",
    },
    secondary: {
      main: "#19857b",
    },
  },
});

export default theme;
